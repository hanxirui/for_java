<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chinal.emp.dao.RoleDao">
    <resultMap id="queryResultMap" type="com.chinal.emp.entity.Role">
                <result column="id" property="id" jdbcType="INTEGER" />
                <result column="level" property="level" jdbcType="INTEGER" />
                <result column="name" property="name" jdbcType="VARCHAR" />
            </resultMap>

    <select id="find" parameterType="org.durcframework.core.expression.ExpressionQuery"
		resultMap="queryResultMap">
		SELECT 
                         id
                , level
                , name
        		FROM role t
		<include refid="expressionBlock.where" />
		<choose>
			<when test="sortname == null">
				ORDER BY t.level desc
			</when>
			<otherwise>
				ORDER BY ${order}
			</otherwise>
		</choose>
		<if test="!isQueryAll">
			LIMIT
			#{start,jdbcType=INTEGER},#{limit,jdbcType=INTEGER}
		</if>
	</select>


    <select id="findTotalCount" parameterType="org.durcframework.core.expression.ExpressionQuery"
		resultType="java.lang.Integer">
		SELECT count(*) FROM role t
		<include refid="expressionBlock.where" />
    </select>

    <insert id="save" parameterType="com.chinal.emp.entity.Role"
    keyProperty="id" keyColumn="id" useGeneratedKeys="true"
    >
	INSERT INTO role
         (
                                                  `level`
                                        , `name`
                              )
	VALUES (
                                                  #{level,jdbcType=INTEGER}
                                        , #{name,jdbcType=VARCHAR}
                     
        )
	</insert>


    <update id="update" parameterType="com.chinal.emp.entity.Role">
    UPDATE role
    SET 
                                                 level=#{level,jdbcType=INTEGER}
                                     , name=#{name,jdbcType=VARCHAR}
                     WHERE id = #{id,jdbcType=INTEGER}
    </update>

    <select id="get" resultMap="queryResultMap" parameterType="com.chinal.emp.entity.Role">
		SELECT 
                         id
                , level
                , name
        		FROM role t
		WHERE id = #{id,jdbcType=INTEGER}
	</select>
	
	<delete id="del" parameterType="com.chinal.emp.entity.Role">
		DELETE FROM role
		WHERE id = #{id,jdbcType=INTEGER}
	</delete>

</mapper>