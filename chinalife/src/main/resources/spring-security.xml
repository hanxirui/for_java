<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	   		http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
			http://www.springframework.org/schema/security 
			http://www.springframework.org/schema/security/spring-security-3.2.xsd">

	<!-- Spring-Security 的配置 -->
	<!-- 注意开启use-expressions.表示开启表达式. see:http://www.family168.com/tutorial/springsecurity3/html/el-access.html -->
	<security:http auto-config="true" use-expressions="true"
		access-denied-page="/denied.do">
		<security:intercept-url pattern="/j_spring_security_check"
			access="permitAll" />
		<security:intercept-url pattern="/login.do"
			access="permitAll" />
		<security:intercept-url pattern="/openEmployee.do"
			access="hasRole('ROLE_5')" />
		<security:intercept-url pattern="/openOrg.do"
			access="hasRole('ROLE_5')" />
		<security:intercept-url pattern="/openRole.do"
			access="hasRole('ROLE_5')" />
		<security:intercept-url pattern="/openCustomer.do"
			access="hasRole('ROLE_1')" />
		<security:intercept-url pattern="/**"
			access="hasRole('ROLE_1')" />

		<security:form-login login-page="/login.do"
			authentication-failure-url="/login.do?error=true" default-target-url="/customer.do"
			authentication-success-handler-ref="authenticationSuccessHandler" />

		<security:logout invalidate-session="true"
			logout-success-url="/login.do" logout-url="/logout.do" />

	</security:http>

	<!-- 指定一个自定义的authentication-manager :customUserDetailsService -->
	<security:authentication-manager>
	<security:authentication-provider user-service-ref="customUserDetailsService">
	      <security:password-encoder ref="passwordEncoder">
			<security:salt-source user-property="username" />
		 </security:password-encoder>
	</security:authentication-provider>
</security:authentication-manager>

	<bean id="authenticationSuccessHandler" class="com.chinal.emp.security.SimpleLoginSuccessHandler">
		    <property name="defaultTargetUrl" value="/openCustomer.do"></property> 
			<property name="forwardToDestination" value="false"></property>
	</bean>

	<!-- 通过 customUserDetailsService,Spring会自动的用户的访问级别. 也可以理解成:以后我们和数据库操作就是通过customUserDetailsService来进行关联. -->
	<bean id="customUserDetailsService" class="com.chinal.emp.security.CustomUserDetailsService">
	</bean>

	<!-- 对密码进行MD5编码 -->
	<bean
		class="org.springframework.security.authentication.encoding.Md5PasswordEncoder"
		id="passwordEncoder" />

</beans>

